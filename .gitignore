node_modules
.vscode
.idea
.DS_Store
*.tgz
dist
playwright-report
test-results
tsconfig.tsbuildinfo

# AI harness
.claude
.codex
.cursor

# OpenWolf: ship the framework (hook scripts + protocol doc), keep the learned
# state local (memory/cerebrum/buglog/anatomy regenerate per checkout).
.wolf/*
!.wolf/OPENWOLF.md
!.wolf/hooks/
# ...but hook runtime state stays local (regenerated each session).
.wolf/hooks/_session.json